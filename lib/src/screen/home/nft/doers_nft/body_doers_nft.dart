
import '../../../../../../index.dart';

class DoersNFTBody extends StatelessWidget {

  const DoersNFTBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: MyText(text: "Doers NFT")),
    );
  }
}